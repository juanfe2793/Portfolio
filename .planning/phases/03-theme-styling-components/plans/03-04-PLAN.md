---
gsd_plan_version: 1.0
phase: 3
slug: landing-page
status: draft
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - src/pages/index.tsx
requirements:
  - THEME-06
autonomous: true
created: 2026-04-03
---

# Plan 3.4 — Landing Page (THEME-06)

Apply the teal theme to the existing landing page structure. Update the profile picture URL to use the new teal color, ensure card tags auto-adopt the new palette, and verify hero section uses the gradient from Plan 3.1.

---

## Tasks

### Task 1 — Update profile picture background color to new teal

**read_first:**
- `/Users/juanfe/code/Portfolio/src/pages/index.tsx` (current state — line 66 has `background=0D9488` which must change to `14b8a6`)

**acceptance_criteria:**
- `grep -n "background=14b8a6" /Users/juanfe/code/Portfolio/src/pages/index.tsx` returns the profile picture line

**action:**
In `src/pages/index.tsx`, line 66, update the profile image URL from `background=0D9488` to `background=14b8a6`:

Change:
```
src="https://ui-avatars.com/api/?name=Juan+Felipe&background=0D9488&color=fff&size=200&rounded=true"
```

To:
```
src="https://ui-avatars.com/api/?name=Juan+Felipe&background=14b8a6&color=fff&size=200&rounded=true"
```

---

### Task 2 — Verify card tags auto-adopt the new teal palette (no JSX changes needed)

**read_first:**
- `/Users/juanfe/code/Portfolio/src/pages/index.module.css` (lines 121–128 — `.cardTag` already uses `--ifm-color-primary-lightest/darkest`)
- `/Users/juanfe/code/Portfolio/.planning/phases/03-theme-styling-components/03-CONTEXT.md` (decision D-18 confirms no JSX changes needed)

**acceptance_criteria:**
- `grep -n "cardTag" /Users/juanfe/code/Portfolio/src/pages/index.module.css` shows it uses `--ifm-color-primary-lightest` and `--ifm-color-primary-darkest`

**action:**
No changes needed. Per decision D-18, `.cardTag` already uses `--ifm-color-primary-lightest/darkest` which will automatically pick up the new teal values from Plan 3.1 once custom.css is updated. Verify the CSS class is intact.

---

### Task 3 — Verify hero gradient is applied (confirmation of Plan 3.1 Task 3)

**read_first:**
- `/Users/juanfe/code/Portfolio/src/pages/index.module.css` (post-Plan-3.1 state — should have hero gradient)

**acceptance_criteria:**
- `grep -n "linear-gradient" /Users/juanfe/code/Portfolio/src/pages/index.module.css` returns teal gradient
- `grep -n "data-theme='dark'" /Users/juanfe/code/Portfolio/src/pages/index.module.css` returns dark mode variant

**action:**
Confirm that Plan 3.1 Task 3 was executed correctly. Check that `.heroBanner` has `background: linear-gradient(135deg, #14b8a6, #0f766e)` and `[data-theme='dark'] .heroBanner` has `background: linear-gradient(135deg, #134e4a, #111827)`.

---

### Task 4 — Ensure social links have hover effect via CSS module

**read_first:**
- `/Users/juanfe/code/Portfolio/src/pages/index.module.css` (lines 148–162 — .socialLink styles)

**acceptance_criteria:**
- `grep -n "opacity" /Users/juanfe/code/Portfolio/src/pages/index.module.css` shows `0.8` to `1.0` transition on hover

**action:**
Verify `.socialLink` CSS already has the 200ms ease opacity transition from 0.8 to 1.0. If not present, add it:

Current (lines 148–162) should read:
```css
.socialLink {
  color: inherit;
  opacity: 0.8;
  transition: opacity 0.2s ease;
}

.socialLink:hover {
  opacity: 1;
  text-decoration: none;
}
```

---

## Verification

```bash
cd /Users/juanfe/code/Portfolio
npm run typecheck
grep -n "background=14b8a6" src/pages/index.tsx
grep -n "linear-gradient" src/pages/index.module.css
grep -n "cardTag" src/pages/index.module.css | grep -c "ifm-color-primary"
```

---

## must_haves

- Profile picture uses teal background (`14b8a6`)
- Hero section renders teal gradient in light mode and dark gradient in dark mode
- Card tags auto-adopt teal palette via existing CSS tokens (no JSX changes)
- Social links have 200ms opacity hover transition
- `npm run typecheck` exits 0
- `npm run build` completes without errors