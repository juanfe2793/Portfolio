---
gsd_plan_version: 1.0
phase: 3
slug: navbar-footer
status: draft
wave: 1
depends_on: []
files_modified:
  - docusaurus.config.ts
requirements:
  - THEME-02
  - THEME-03
autonomous: true
created: 2026-04-03
---

# Plan 3.2 — Navbar & Footer (THEME-02, THEME-03)

Fix the navbar (CV/Resume link type, "Docs" → "Guides" rename, correct ordering) and simplify the footer to a two-column layout (Content + Connect) per UI-SPEC specifications.

---

## Tasks

### Task 1 — Fix navbar CV/Resume link and rename "Docs" to "Guides"

**read_first:**
- `/Users/juanfe/code/Portfolio/docusaurus.config.ts` (current state — navbar items at lines 107–126)
- `/Users/juanfe/code/Portfolio/.planning/phases/03-theme-styling-components/03-CONTEXT.md` (decisions D-04, D-05)

**acceptance_criteria:**
- `grep -n "CV / Resume" /Users/juanfe/code/Portfolio/docusaurus.config.ts` shows `to: '/docs/portfolio/cv'` (NOT `type: 'docSidebar'`)
- `grep -n "Docs" /Users/juanfe/code/Portfolio/docusaurus.config.ts` returns `label: 'Guides'` (no label: 'Docs' remaining)
- Navbar order: Guides | Blog | CV / Resume (left) + GitHub (right)

**action:**
In `docusaurus.config.ts`, replace the entire `navbar.items` array (lines 107–126) with:

```typescript
items: [
  {
    to: '/docs/guides/',
    label: 'Guides',
    position: 'left',
  },
  {to: '/blog', label: 'Blog', position: 'left'},
  {
    to: '/docs/portfolio/cv',
    label: 'CV / Resume',
    position: 'left',
  },
  {
    href: 'https://github.com/juanfe2793/Portfolio',
    label: 'GitHub',
    position: 'right',
  },
],
```

---

### Task 2 — Simplify footer to two columns (Content + Connect)

**read_first:**
- `/Users/juanfe/code/Portfolio/docusaurus.config.ts` (current state — footer at lines 128–164)
- `/Users/juanfe/code/Portfolio/.planning/phases/03-theme-styling-components/03-UI-SPEC.md` (footer columns from lines 263–268)

**acceptance_criteria:**
- `grep -c "title: 'Content'" /Users/juanfe/code/Portfolio/docusaurus.config.ts` returns 1
- `grep -c "title: 'Connect'" /Users/juanfe/code/Portfolio/docusaurus.config.ts` returns 1
- Footer has exactly 2 link groups (not 3)
- Footer copyright line is present

**action:**
In `docusaurus.config.ts`, replace the `footer.links` array (lines 130–162) with:

```typescript
links: [
  {
    title: 'Content',
    items: [
      {
        label: 'Guides',
        to: '/docs/guides/',
      },
      {
        label: 'Blog',
        to: '/blog',
      },
      {
        label: 'CV',
        to: '/docs/portfolio/cv',
      },
    ],
  },
  {
    title: 'Connect',
    items: [
      {
        label: 'LinkedIn',
        href: 'https://linkedin.com/in/juangomez27',
      },
      {
        label: 'GitHub',
        href: 'https://github.com/juanfe2793',
      },
      {
        label: 'Email',
        href: 'mailto:hello@felipegomez.me',
      },
    ],
  },
],
```

---

### Task 3 — Add aria-label to social link anchors in `src/pages/index.tsx`

**read_first:**
- `/Users/juanfe/code/Portfolio/src/pages/index.tsx` (current state — social links at lines 77–95)
- `/Users/juanfe/code/Portfolio/.planning/phases/03-theme-styling-components/03-UI-SPEC.md` (social link aria-labels from lines 188–198)

**acceptance_criteria:**
- `grep -n 'aria-label="GitHub' /Users/juanfe/code/Portfolio/src/pages/index.tsx` returns line with `aria-label="GitHub profile"`
- `grep -n 'aria-label="LinkedIn' /Users/juanfe/code/Portfolio/src/pages/index.tsx` returns line with `aria-label="LinkedIn profile"`
- `grep -n 'aria-label="Send email' /Users/juanfe/code/Portfolio/src/pages/index.tsx` returns line with `aria-label="Send email"`
- Each SVG icon has `aria-hidden="true"`

**action:**
In `src/pages/index.tsx`, update the three social link anchors (lines 77–95) to add aria-labels:

GitHub anchor becomes:
```tsx
<a
  href="https://github.com/juanfe2793"
  aria-label="GitHub profile"
  className={styles.socialLink}>
  <GitHubIcon />
</a>
```

LinkedIn anchor becomes:
```tsx
<a
  href="https://linkedin.com/in/juangomez27"
  aria-label="LinkedIn profile"
  className={styles.socialLink}>
  <LinkedInIcon />
</a>
```

Email anchor becomes:
```tsx
<a
  href="mailto:hello@felipegomez.me"
  aria-label="Send email"
  className={styles.socialLink}>
  <EmailIcon />
</a>
```

Add `aria-hidden="true"` to each `<GitHubIcon />`, `<LinkedInIcon />`, `<EmailIcon />` JSX usage.

---

## Verification

```bash
cd /Users/juanfe/code/Portfolio
npm run typecheck
grep -c "to: '/docs/portfolio/cv'" docusaurus.config.ts  # must return 1
grep -c "label: 'Guides'" docusaurus.config.ts  # must return 1
grep -c "title: 'Connect'" docusaurus.config.ts  # must return 1
grep -c 'aria-label="GitHub' src/pages/index.tsx  # must return 1
```

---

## must_haves

- Navbar "CV / Resume" points to `/docs/portfolio/cv` (not docSidebar)
- Navbar has "Guides" not "Docs"
- Footer has two columns: Content (Guides, Blog, CV) + Connect (LinkedIn, GitHub, Email)
- Social links have accessible aria-labels
- `npm run typecheck` exits 0