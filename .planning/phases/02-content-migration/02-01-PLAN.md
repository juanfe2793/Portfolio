---
phase: 02-content-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - v2/docs/portfolio/cv.mdx
  - v2/static/css/extra.css
  - v2/blog/authors.yml
  - v2/blog/tags.yml
autonomous: true
requirements:
  - CONT-01
  - CONT-05
must_haves:
  truths:
    - "All Docusaurus scaffold placeholder content is deleted before any migration"
    - "CV page renders in Docusaurus at /docs/portfolio/cv path"
    - "Static assets from MkDocs are available in v2/static/"
  artifacts:
    - path: "v2/docs/portfolio/cv.mdx"
      provides: "CV page with Docusaurus-compatible MDX"
      contains: "Juan Felipe"
    - path: "v2/static/css/extra.css"
      provides: "MkDocs custom CSS carried over"
  key_links:
    - from: "v2/docs/portfolio/cv.mdx"
      to: "Docusaurus docs plugin"
      via: "file in docs/ directory auto-discovered"
      pattern: "title:"
---

<objective>
Clear all Docusaurus scaffold placeholder content, migrate the CV page from MkDocs to Docusaurus MDX, and copy static assets.

Purpose: Establish a clean slate for all content migration and deliver the first migrated page (CV).
Output: `v2/docs/portfolio/cv.mdx`, static assets in `v2/static/`, placeholder content removed.
</objective>

<execution_context>
@/Users/juanfe/code/Portfolio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juanfe/code/Portfolio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-migration/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear placeholder content and copy static assets</name>
  <files>v2/blog/, v2/docs/, v2/static/css/extra.css</files>
  <read_first>
    - v2/blog/2019-05-28-first-blog-post.md
    - v2/docs/intro.md
    - docs/assets/css/extra.css
  </read_first>
  <action>
Delete ALL Docusaurus scaffold placeholder files (per D-08):

Blog placeholders to delete:
- `v2/blog/2019-05-28-first-blog-post.md`
- `v2/blog/2019-05-29-long-blog-post.md`
- `v2/blog/2021-08-01-mdx-blog-post.mdx`
- `v2/blog/2021-08-26-welcome/` (entire directory)

Docs placeholders to delete:
- `v2/docs/intro.md`
- `v2/docs/tutorial-basics/` (entire directory)
- `v2/docs/tutorial-extras/` (entire directory)

Replace `v2/blog/tags.yml` with tags matching the blog categories used in MkDocs:

```yaml
engineering:
  label: Engineering
  permalink: /engineering
  description: Engineering and infrastructure topics

career:
  label: Career
  permalink: /career
  description: Career development and professional growth
```

Copy static assets from MkDocs to Docusaurus:
- Copy `docs/assets/css/extra.css` to `v2/static/css/extra.css`

Note: `v2/blog/authors.yml` will be replaced in Task 2. Keep it for now.
  </action>
  <verify>
    <automated>test ! -f v2/docs/intro.md && test ! -d v2/docs/tutorial-basics && test ! -d v2/docs/tutorial-extras && test ! -f v2/blog/2019-05-28-first-blog-post.md && test ! -f v2/blog/2019-05-29-long-blog-post.md && test ! -f v2/blog/2021-08-01-mdx-blog-post.mdx && test ! -d v2/blog/2021-08-26-welcome && test -f v2/static/css/extra.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - v2/docs/intro.md does NOT exist
    - v2/docs/tutorial-basics/ directory does NOT exist
    - v2/docs/tutorial-extras/ directory does NOT exist
    - v2/blog/2019-05-28-first-blog-post.md does NOT exist
    - v2/blog/2019-05-29-long-blog-post.md does NOT exist
    - v2/blog/2021-08-01-mdx-blog-post.mdx does NOT exist
    - v2/blog/2021-08-26-welcome/ directory does NOT exist
    - v2/static/css/extra.css exists and contains CSS content
    - v2/blog/tags.yml contains `engineering:` and `career:` entries (no `facebook:` or `hello:`)
  </acceptance_criteria>
  <done>All scaffold placeholders removed. Static assets copied. Tags file updated with project-relevant tags.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CV page and authors.yml</name>
  <files>v2/docs/portfolio/cv.mdx, v2/blog/authors.yml</files>
  <read_first>
    - docs/portfolio/cv.md
    - v2/blog/authors.yml
    - v2/docusaurus.config.ts
  </read_first>
  <action>
Create `v2/docs/portfolio/cv.mdx` by converting `docs/portfolio/cv.md` with these transformations (per D-01, D-02, D-03, D-04):

1. Add Docusaurus frontmatter at top:
```yaml
---
title: Curriculum Vitae
sidebar_label: CV / Resume
sidebar_position: 1
---
```

2. Replace the MkDocs Material title line `# :material-file-document: Curriculum Vitae` with `# Curriculum Vitae` (remove icon shortcode).

3. Remove the `<div class="grid" markdown>` wrapper and inner `<div markdown>` / `<div markdown align="right">` tags entirely. Keep the content but flatten the structure (per D-01 -- simplified layout, Phase 3 adds MDX grid back).

4. Replace icon link shortcodes (per D-02):
   - `[:fontawesome-brands-linkedin: LinkedIn](https://linkedin.com/in/juangomez27)` becomes `[LinkedIn](https://linkedin.com/in/juangomez27)`
   - `[:fontawesome-brands-github: GitHub](https://github.com/juanfe2793)` becomes `[GitHub](https://github.com/juanfe2793)`
   - `[:fontawesome-solid-envelope: Email](mailto:hello@felipegomez.me)` becomes `[Email](mailto:hello@felipegomez.me)`
   - Format as: `[LinkedIn](https://linkedin.com/in/juangomez27) | [GitHub](https://github.com/juanfe2793) | [Email](mailto:hello@felipegomez.me)`

5. Convert the admonition (per D-03):
   - MkDocs: `!!! success "Key Achievements"` followed by indented content
   - Docusaurus: `:::success[Key Achievements]` followed by content, closed with `:::`
   - The 3 bullet points inside the admonition must be between `:::success[Key Achievements]` and `:::`

6. Keep all other markdown content (experience sections, skills table, education, certifications) exactly as-is.

Replace `v2/blog/authors.yml` entirely with Juan Felipe's author entry (per D-05):

```yaml
juanfe:
  name: Juan Felipe Gomez Manzanares
  title: Staff Software Engineer
  url: https://linkedin.com/in/juangomez27
  image_url: https://github.com/juanfe2793.png
  page: true
  socials:
    linkedin: juangomez27
    github: juanfe2793
```
  </action>
  <verify>
    <automated>cd /Users/juanfe/code/Portfolio/v2 && test -f docs/portfolio/cv.mdx && grep -q "title: Curriculum Vitae" docs/portfolio/cv.mdx && grep -q ":::success" docs/portfolio/cv.mdx && grep -q "Juan Felipe" docs/portfolio/cv.mdx && ! grep -q ":material-" docs/portfolio/cv.mdx && ! grep -q ":fontawesome-" docs/portfolio/cv.mdx && ! grep -q '<div class="grid"' docs/portfolio/cv.mdx && grep -q "juanfe:" blog/authors.yml && grep -q "page: true" blog/authors.yml && grep -q "juanfe2793.png" blog/authors.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - v2/docs/portfolio/cv.mdx exists
    - cv.mdx contains `title: Curriculum Vitae` in frontmatter
    - cv.mdx contains `:::success[Key Achievements]` (Docusaurus admonition syntax)
    - cv.mdx contains `:::` closing tag after the Key Achievements bullets
    - cv.mdx does NOT contain `:material-file-document:` or any `:fontawesome-` shortcodes
    - cv.mdx does NOT contain `<div class="grid"` or `<div markdown`
    - cv.mdx contains `[LinkedIn](https://linkedin.com/in/juangomez27) | [GitHub](https://github.com/juanfe2793) | [Email](mailto:hello@felipegomez.me)`
    - v2/blog/authors.yml contains `juanfe:` as first key
    - v2/blog/authors.yml contains `page: true`
    - v2/blog/authors.yml contains `image_url: https://github.com/juanfe2793.png`
    - v2/blog/authors.yml does NOT contain `yangshun` or `slorber`
  </acceptance_criteria>
  <done>CV page migrated to MDX with all MkDocs syntax converted. Authors file replaced with Juan Felipe's profile.</done>
</task>

<task type="auto">
  <name>Task 3: Verify build passes with CV page</name>
  <files></files>
  <read_first>
    - v2/docusaurus.config.ts
  </read_first>
  <action>
Run `npm run build` in `v2/` directory to verify:
- The CV page builds without errors
- No broken links (onBrokenLinks: 'throw' is active)
- The blog with updated authors.yml builds cleanly

If the build fails due to broken internal links (e.g., the sidebar still referencing `intro` or tutorials), check if `sidebars.ts` needs updating. The `tutorialSidebar` with `autogenerated: true` should automatically reflect the new file structure, but if it references deleted files, update accordingly.

If build warns about untruncated blog posts (since placeholder blogs were deleted, this should not happen), fix as needed.
  </action>
  <verify>
    <automated>cd /Users/juanfe/code/Portfolio/v2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in v2/ exits with code 0
    - Build output contains "Build optimized" or similar success message
    - No "broken link" errors in build output
  </acceptance_criteria>
  <done>Docusaurus builds cleanly with CV page, cleared placeholders, and updated authors/tags.</done>
</task>

</tasks>

<verification>
- `npm run build` passes in v2/ with zero errors
- No placeholder scaffold files remain in v2/docs/ or v2/blog/
- CV page accessible at /docs/portfolio/cv path in build output
- authors.yml has Juan Felipe's profile, not Docusaurus defaults
</verification>

<success_criteria>
- All Docusaurus placeholder content deleted
- CV renders correctly with no MkDocs-specific syntax remaining
- Static assets available in v2/static/
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-migration/02-01-SUMMARY.md`
</output>
