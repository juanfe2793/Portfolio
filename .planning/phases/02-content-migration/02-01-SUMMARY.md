---
phase: 02-content-migration
plan: "01"
subsystem: docs-structure
tags:
  - sidebar
  - navbar
  - cv
  - docusaurus
dependency_graph:
  requires: []
  provides:
    - guidesSidebar scoped to docs/guides/
    - CV page rendered without sidebar panel
    - Navbar order locked: CV/Resume → Guides → Blog → GitHub
  affects:
    - docs/portfolio/cv.mdx
    - sidebars.ts
    - docusaurus.config.ts
tech_stack:
  added: []
  patterns:
    - displayed_sidebar null frontmatter to suppress sidebar on standalone pages
    - Direct to: navbar links instead of type:docSidebar for sidebar-free routing
key_files:
  created: []
  modified:
    - sidebars.ts
    - docs/portfolio/cv.mdx
    - docusaurus.config.ts
decisions:
  - "guidesSidebar with dirName:'guides' scopes autogeneration to docs/guides/ only — CV excluded from sidebar tree"
  - "displayed_sidebar:null on CV frontmatter renders page without sidebar panel per D-01/D-03"
  - "Navbar uses direct to: links (not type:docSidebar) for both CV and Guides per THEME-02"
metrics:
  duration: "~10 minutes"
  completed_date: "2026-04-07"
  tasks_completed: 3
  files_modified: 3
---

# Phase 2 Plan 1: Sidebar Restructure and Navbar Lock Summary

**One-liner:** Scoped guidesSidebar to docs/guides/, suppressed CV sidebar panel via displayed_sidebar null, and locked navbar to CV/Resume → Guides → Blog → GitHub order using direct links.

## What Was Built

Three focused configuration changes to decouple the CV page from the guides sidebar hierarchy and lock the navbar order per Phase 3 context decisions:

1. **sidebars.ts** — Replaced the single `tutorialSidebar` (autogenerated from `.`) with `guidesSidebar` (autogenerated from `guides`). The CV page is no longer included in any sidebar tree.

2. **docs/portfolio/cv.mdx frontmatter** — Added `displayed_sidebar: null`, `hide_table_of_contents: false`, and `custom_edit_url: null`. The CV renders as a standalone page with no sidebar panel but retains the right-side TOC for navigation within the long CV content.

3. **docusaurus.config.ts navbar** — Replaced `type: 'docSidebar'` / `sidebarId: 'tutorialSidebar'` items with direct `to:` links. Final order: CV/Resume (`/docs/portfolio/cv`) → Guides (`/docs/guides`) → Blog (`/blog`) → GitHub (right-aligned href).

## Verification

- `grep guidesSidebar sidebars.ts` — found; `grep tutorialSidebar sidebars.ts` — not found
- `grep "displayed_sidebar: null" docs/portfolio/cv.mdx` — found on line 5
- `grep "tutorialSidebar" docusaurus.config.ts` — 0 matches; CV and Guides direct links confirmed
- `npm run typecheck` — exits 0, no type errors

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1 | 0ff3dd8 | feat(02-01): split sidebars.ts — guidesSidebar only, remove tutorialSidebar |
| 2 | a3f1fe3 | feat(02-01): add displayed_sidebar null to CV frontmatter |
| 3 | 0e657fb | feat(02-01): update navbar — correct order and replace docSidebar with direct links |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all changes are configuration wiring with no placeholder data.

## Self-Check: PASSED

- sidebars.ts: `guidesSidebar` present, `tutorialSidebar` absent, `dirName: 'guides'` confirmed
- docs/portfolio/cv.mdx: `displayed_sidebar: null` on line 5, `custom_edit_url: null` present
- docusaurus.config.ts: no `type: 'docSidebar'`, no `sidebarId: 'tutorialSidebar'`, CV and Guides direct links present
- Commits 0ff3dd8, a3f1fe3, 0e657fb all exist in git log
- `npm run typecheck` exits 0
